include ../base/variables.pug
include ../base/mixins.pug

section.count 
    .container.my-4
        .row 
            each countItem in countList
                .col-12.col-md-6.col-lg-3.text-center
                    +cardCustom(countItem.image, none, none, countItem.title, countItem.des, countItem.alt, countItem.color)